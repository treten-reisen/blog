---
import type { StrapiBlockRichText } from "../../data/schema/blocks.schema"

import "./rich-text.css"

export type Props = {
  data: StrapiBlockRichText
}

const { data } = Astro.props as Props
---

<div class="prose font-serif" set:html={data.html} />
